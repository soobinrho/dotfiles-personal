## How to deploy os-main

Ansible takes care of the vast majority of os-main deployment.
To run Ansible, install Python from https://www.python.org/downloads/windows/

```pwsh
# Install Ansible.
py -m pip install ansible

# Copy and paste the following code block as `setup-os-main.playbook.yaml`.
```
```yaml

```
```pwsh
ansible-playbook setup-os-main.playbook.yaml
```

<br>
